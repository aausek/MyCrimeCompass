import React from 'react';
import CrimeBlocks from "../components/CrimeBlocks"

// Query: Average duration of specific crime per month/year
const CrimeBlocksView = () => (
    <div>
        <CrimeBlocks>
            
        </CrimeBlocks>
    </div>
)

export default CrimeBlocksView;